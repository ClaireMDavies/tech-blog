<h1>Welcome to my Blog of Tech</h1>

<!-- Loop over each object in the projects array -->
{{#each posts as |post| }}
<div>
    <h2>{{post.title}}</h2>
    <p>{{post.post_content}}</p>
    {{user.first_name}}
</div>
{{/each}} 